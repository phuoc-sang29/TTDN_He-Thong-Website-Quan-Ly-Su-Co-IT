{
  "name": "helpdesk-project",
  "version": "1.0.0",
  "description": "Hệ thống quản lý sự cố và hỗ trợ kỹ thuật nội bộ — hỗ trợ đa vai trò, chat AI tích hợp và quản lý hợp đồng cho thuê thiết bị.",
  "main": "index.js",
  "scripts": {
    "dev":      "concurrently -n backend,frontend -c cyan,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "backend":  "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
